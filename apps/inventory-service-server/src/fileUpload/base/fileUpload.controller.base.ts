/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { FileUploadService } from "../fileUpload.service";
import { FileUploadCreateInput } from "./FileUploadCreateInput";
import { FileUpload } from "./FileUpload";
import { FileUploadFindManyArgs } from "./FileUploadFindManyArgs";
import { FileUploadWhereUniqueInput } from "./FileUploadWhereUniqueInput";
import { FileUploadUpdateInput } from "./FileUploadUpdateInput";
import { ProgressTrackerFindManyArgs } from "../../progressTracker/base/ProgressTrackerFindManyArgs";
import { ProgressTracker } from "../../progressTracker/base/ProgressTracker";
import { ProgressTrackerWhereUniqueInput } from "../../progressTracker/base/ProgressTrackerWhereUniqueInput";

export class FileUploadControllerBase {
  constructor(protected readonly service: FileUploadService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: FileUpload })
  async createFileUpload(
    @common.Body() data: FileUploadCreateInput
  ): Promise<FileUpload> {
    return await this.service.createFileUpload({
      data: data,
      select: {
        createdAt: true,
        fileName: true,
        filePath: true,
        id: true,
        status: true,
        updatedAt: true,
        uploadStatus: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [FileUpload] })
  @ApiNestedQuery(FileUploadFindManyArgs)
  async fileUploads(@common.Req() request: Request): Promise<FileUpload[]> {
    const args = plainToClass(FileUploadFindManyArgs, request.query);
    return this.service.fileUploads({
      ...args,
      select: {
        createdAt: true,
        fileName: true,
        filePath: true,
        id: true,
        status: true,
        updatedAt: true,
        uploadStatus: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: FileUpload })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async fileUpload(
    @common.Param() params: FileUploadWhereUniqueInput
  ): Promise<FileUpload | null> {
    const result = await this.service.fileUpload({
      where: params,
      select: {
        createdAt: true,
        fileName: true,
        filePath: true,
        id: true,
        status: true,
        updatedAt: true,
        uploadStatus: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: FileUpload })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateFileUpload(
    @common.Param() params: FileUploadWhereUniqueInput,
    @common.Body() data: FileUploadUpdateInput
  ): Promise<FileUpload | null> {
    try {
      return await this.service.updateFileUpload({
        where: params,
        data: data,
        select: {
          createdAt: true,
          fileName: true,
          filePath: true,
          id: true,
          status: true,
          updatedAt: true,
          uploadStatus: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: FileUpload })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteFileUpload(
    @common.Param() params: FileUploadWhereUniqueInput
  ): Promise<FileUpload | null> {
    try {
      return await this.service.deleteFileUpload({
        where: params,
        select: {
          createdAt: true,
          fileName: true,
          filePath: true,
          id: true,
          status: true,
          updatedAt: true,
          uploadStatus: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/progressTrackers")
  @ApiNestedQuery(ProgressTrackerFindManyArgs)
  async findProgressTrackers(
    @common.Req() request: Request,
    @common.Param() params: FileUploadWhereUniqueInput
  ): Promise<ProgressTracker[]> {
    const query = plainToClass(ProgressTrackerFindManyArgs, request.query);
    const results = await this.service.findProgressTrackers(params.id, {
      ...query,
      select: {
        createdAt: true,
        currentIndex: true,

        fileUpload: {
          select: {
            id: true,
          },
        },

        id: true,
        progressStatus: true,
        status: true,
        totalItems: true,
        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/progressTrackers")
  async connectProgressTrackers(
    @common.Param() params: FileUploadWhereUniqueInput,
    @common.Body() body: ProgressTrackerWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      progressTrackers: {
        connect: body,
      },
    };
    await this.service.updateFileUpload({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/progressTrackers")
  async updateProgressTrackers(
    @common.Param() params: FileUploadWhereUniqueInput,
    @common.Body() body: ProgressTrackerWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      progressTrackers: {
        set: body,
      },
    };
    await this.service.updateFileUpload({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/progressTrackers")
  async disconnectProgressTrackers(
    @common.Param() params: FileUploadWhereUniqueInput,
    @common.Body() body: ProgressTrackerWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      progressTrackers: {
        disconnect: body,
      },
    };
    await this.service.updateFileUpload({
      where: params,
      data,
      select: { id: true },
    });
  }
}
